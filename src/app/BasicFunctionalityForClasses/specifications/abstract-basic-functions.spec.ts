import { AbstractBasicFunctions } from './abstract-basic-functions';

describe('AbstractBasicFunctions', () => {
  it('should create an instance', () => {
    expect(new AbstractBasicFunctions()).toBeTruthy();
  });
});
